.TH _PRINTF 3 "JANUARY 2026" "Custom Printf" "Custom Library"
.SH NAME
_printf \- formatted output conversion
.SH SYNOPSIS
.B #include "main.h"
.sp
.B int _printf(const char *format, ...);
.SH DESCRIPTION
The
.B _printf()
function produces output according to a
.I format
as described below. It writes output to
.B stdout
(the standard output stream).
.PP
The function returns the number of characters printed (excluding the null byte used to end output to strings).
.SH FORMAT
The format string is composed of zero or more directives. Each directive consists of the
.B %
character, followed by a conversion specifier.
.SH CONVERSION SPECIFIERS
.TP
.B c
The
.B int
argument is converted to an
.B unsigned char
and the resulting character is written.
.TP
.B s
The
.B char *
argument is expected to be a pointer to an array of characters (string).
.TP
.B d, i
The
.B int
argument is converted to signed decimal notation.
.TP
.B %
A
.B %
is written. No argument is converted.
.SH RETURN VALUE
Upon successful return, the
.B _printf()
function returns the number of characters printed. If an output error is encountered, a value of -1 is returned.
.SH EXAMPLES
.B _printf("Character: [%c]\\n", 'H');
.br
.B _printf("String: [%s]\\n", "I am a string!");
.br
.B _printf("Decimal: [%d]\\n", 1024);
.SH SEE ALSO
printf(3)
.SH AUTHOR
David Abdurahmanov (vitasec)
